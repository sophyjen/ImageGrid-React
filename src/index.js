import React from "react"
import ReactDOM from "react-dom/client"
import ImageGrid from "./ImageComp"


const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(<ImageGrid></ImageGrid>)